{
  "name": "hardcore-ai-landing",
  "version": "2.0.0",
  "description": "Hardcore AI Landing Page — Cloudflare Pages + Functions",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev public --port 3000",
    "deploy": "npx wrangler pages deploy public"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "wrangler": "^3.60.0"
  }
}
